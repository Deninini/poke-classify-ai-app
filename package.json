{
  "name": "pokemon-image-classifier-ai-web-app",
  "private": true,
  "scripts": {
    "build": "mkdir -p public && bun build src/script.ts --outdir public",
    "start": "vercel dev"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
